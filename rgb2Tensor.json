{
  "description": "This is package description",
  "enable": true,
  "nodes": {
    "rgb2Tensor": {
      "type": "rgb2Tensor",
      "category": "function",
      "title": "rgb2Tensor",
      "tooltip": "This is node tooltip",
      "externalImports": "import torch\nimport torchvision.transforms as transforms\nfrom PIL import Image",
      "codeGenerator": "function code(inputs, outputs, node, generator) {\n          const code = `img = Image.open(${inputs[1]})\\ntransform = transforms.ToTensor()\\n${outputs[1]} = transform(img).unsqueeze(0)\\ndef image_info(image):\\n    print(\"--- Original Image: ---\")\\n    # Remove the batch dimension and convert the tensor to an image\\n    image = transforms.ToPILImage()(image.squeeze(0))\\n    # Display the image\\n    plt.imshow(image)\\n    plt.axis('off')\\n    plt.show()\\n    print (\"--------------------------------\")\\nimage_info(${outputs[1]});\\n${outputs[1]} = {\\n  'value': ${outputs[1]},\\n  'dataType': 'Tensor',\\n  'metadata': {\\n    'colorChannel': 'rgb',\\n    'channelOrder': 'channelLast',\\n    'isMiniBatched': False,\\n    'intensityRange': '0-255',\\n    'device': 'cpu'\\n  }\\n}\\n${outputs[0]}`;\n          return code;\n        }",
      "inputs": {
        "execIn": {
          "title": "execIn",
          "tooltip": "execIn",
          "dataType": "exec",
          "showWidget": false,
          "showTitle": false
        },
        "input_image": {
          "title": "path",
          "dataType": "string",
          "tooltip": "input image."
        }
      },
      "outputs": {
        "execOut": {
          "title": "execOut",
          "tooltip": "execOut",
          "dataType": "exec",
          "showWidget": false,
          "showTitle": false
        },
        "output_image": {
          "title": "image",
          "dataType": "image",
          "defaultValue": {
            "dataType": "torch.tensor"
          },
          "tooltip": "{dataType: torch.tensor, value, layout: [chw], colorMode: [rgb, grayscale], intensityRange: 0-255' device: cpu}"
        }
      }
    }
  }
}